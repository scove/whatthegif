{
  "name": "whatthegif",
  "version": "1.0.0",
  "description": "The program will select one player to be the judge, the judge will draw a random prompt from an array and remove that random prompt from the array. Players will then react by entering a noun/verb that will use the gifhy(?) to pull a random reaction to the console. The judge will vote on the best reaction, which will increase the player selected's score. Once the user gets to three points, they win.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/scove/whatthegif.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/scove/whatthegif/issues"
  },
  "homepage": "https://github.com/scove/whatthegif#readme",
  "devDependencies": {
    "electron": "^3.0.2"
  },
  "dependencies": {
    "giphy-js-sdk-core": "^1.0.5",
    "electron-context-menu": "^0.10.0"
  }
}
